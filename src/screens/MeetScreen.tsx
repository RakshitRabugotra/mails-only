import React from 'react';
import { View } from 'react-native';
import { Text } from 'react-native-paper';

export default function MeetScreen() {
  return (
    <View style={{ padding: 20 }}>
      <Text variant="titleLarge">Google Meet Screen (Mock)</Text>
    </View>
  );
}
